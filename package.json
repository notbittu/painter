{
  "name": "painter",
  "version": "1.0.0",
  "description": "Painter application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p build && cd client && npm install && npm run build && cd .. && cp -r client/build/* build/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": "16.x"
  }
}
