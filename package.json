{
  "name": "painter",
  "version": "1.0.0",
  "description": "Painter application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build && cd .. && mkdir -p static/client/build && cp -r client/build/* static/client/build/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.17.0 <19.0.0"
  }
}
