services:
  - type: web
    name: painter
    env: node
    buildCommand: npm run heroku-postbuild
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: NODE_ENV
        value: production 